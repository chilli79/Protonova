<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE RIVAL PROTOCOL</title>
    <style>
        :root {
            --bg-color: #050505;
            --text-main: #e0e0e0;
            --accent-green: #00ff41;
            --accent-red: #ff3333;
            --dim-gray: #333;
            --font-mono: 'Courier New', Courier, monospace;
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: var(--font-mono);
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            height: 100vh;
            overflow: hidden;
        }

        /* HEADER */
        header {
            border-bottom: 1px solid var(--dim-gray);
            padding-bottom: 20px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        h1 {
            font-size: 1.5rem;
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .status-badge {
            border: 1px solid var(--accent-green);
            color: var(--accent-green);
            padding: 5px 10px;
            font-size: 0.8rem;
            animation: pulse 2s infinite;
        }

        /* MAIN LAYOUT */
        .dashboard {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 20px;
            height: 100%;
        }

        .panel {
            border: 1px solid var(--dim-gray);
            padding: 20px;
            overflow-y: auto;
            background: rgba(255, 255, 255, 0.02);
        }

        h2 {
            font-size: 1rem;
            border-bottom: 1px solid var(--dim-gray);
            padding-bottom: 10px;
            margin-top: 0;
            color: var(--accent-green);
        }

        /* PANEL 1: THE CONTRACT (Rival Specs) */
        .contract-list {
            list-style: none;
            padding: 0;
        }
        .contract-list li {
            margin-bottom: 15px;
            font-size: 0.9rem;
            line-height: 1.4;
        }
        .highlight { color: var(--accent-green); font-weight: bold; }
        .alert { color: var(--accent-red); }

        /* PANEL 2: VLOG / JOURNAL */
        #vlog-input {
            width: 100%;
            height: 100px;
            background: #111;
            border: 1px solid var(--dim-gray);
            color: white;
            padding: 10px;
            font-family: var(--font-mono);
            margin-bottom: 10px;
        }
        
        button {
            background: var(--dim-gray);
            color: white;
            border: none;
            padding: 10px 20px;
            font-family: var(--font-mono);
            cursor: pointer;
            transition: 0.3s;
        }
        button:hover { background: var(--accent-green); color: black; }

        .log-history {
            margin-top: 20px;
            font-size: 0.8rem;
            border-top: 1px solid var(--dim-gray);
            padding-top: 10px;
        }

        /* PANEL 3: #100 RULE GRID */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(10, 1fr);
            gap: 5px;
        }
        .day-box {
            aspect-ratio: 1;
            border: 1px solid var(--dim-gray);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 0.7rem;
            transition: 0.2s;
        }
        .day-box.active {
            background-color: var(--accent-green);
            color: black;
            border-color: var(--accent-green);
        }
        .progress-text {
            margin-top: 10px;
            font-size: 0.8rem;
            text-align: right;
        }

        /* FOOTER (Focus Tools) */
        .focus-bar {
            margin-top: 20px;
            border-top: 1px solid var(--dim-gray);
            padding-top: 15px;
            display: flex;
            justify-content: space-around;
        }
        .timer-display {
            font-size: 1.2rem;
            font-weight: bold;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }
    </style>
</head>
<body>

    <header>
        <div>
            <h1>RIVAL PROTOCOL v1.0</h1>
            <small id="date-display"></small>
        </div>
        <div class="status-badge">RIVAL STATUS: WORKING</div>
    </header>

    <div class="dashboard">
        
        <div class="panel">
            <h2>// THE STANDARD</h2>
            <ul class="contract-list">
                <li>
                    <span class="highlight">IDENTITY:</span><br>
                    Same genetics. Same mind. <span class="alert">Different action.</span>
                </li>
                <li>
                    <span class="highlight">PROTOCOL 1:</span><br>
                    Execute boring work despite craving stimulation.
                </li>
                <li>
                    <span class="highlight">PROTOCOL 2:</span><br>
                    Exercise even when the body says no.
                </li>
                <li>
                    <span class="highlight">PROTOCOL 3:</span><br>
                    "Fuck tiredness. I'm doing it anyway."
                </li>
            </ul>
            <hr style="border-color: #333;">
            <p style="font-size: 0.8rem; color: #888;">
                "The only person stopping you is you."
            </p>
        </div>

        <div class="panel">
            <h2>// DAILY REPORT</h2>
            <p style="font-size: 0.8rem;">Did you beat the Rival today?</p>
            
            <textarea id="vlog-input" placeholder="Log your wins/losses here. Be honest."></textarea>
            <button onclick="saveLog()">COMMIT ENTRY</button>

            <div class="log-history" id="log-history">
                </div>
        </div>

        <div class="panel">
            <h2>// #100 RULE</h2>
            <div class="grid-container" id="grid">
                </div>
            <div class="progress-text" id="progress-text">STREAK: 0/100</div>
        </div>
    </div>

    <div class="focus-bar">
        <div>
            <span>AUDIO MASK:</span>
            <button onclick="toggleNoise()">BROWN NOISE [I/O]</button>
        </div>
        <div>
            <span>FOCUS TIMER:</span>
            <span class="timer-display" id="timer">30:00</span>
            <button onclick="startTimer()">START</button>
            <button onclick="resetTimer()">RESET</button>
        </div>
    </div>

    <script>
        // 1. DATE DISPLAY
        const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
        document.getElementById('date-display').innerText = new Date().toLocaleDateString('en-US', options);

        // 2. THE #100 RULE GRID
        const grid = document.getElementById('grid');
        const progressText = document.getElementById('progress-text');
        
        // Load state from local storage or create empty array
        let gridState = JSON.parse(localStorage.getItem('rivalGridState')) || new Array(100).fill(false);

        function renderGrid() {
            grid.innerHTML = '';
            let completedCount = 0;

            gridState.forEach((isActive, index) => {
                const box = document.createElement('div');
                box.classList.add('day-box');
                box.innerText = index + 1;
                if (isActive) {
                    box.classList.add('active');
                    completedCount++;
                }
                
                // Click to toggle
                box.onclick = () => {
                    gridState[index] = !gridState[index];
                    localStorage.setItem('rivalGridState', JSON.stringify(gridState));
                    renderGrid();
                };

                grid.appendChild(box);
            });

            progressText.innerText = `STREAK: ${completedCount}/100`;
        }
        renderGrid();

        // 3. DAILY LOG SYSTEM
        const logInput = document.getElementById('vlog-input');
        const logHistory = document.getElementById('log-history');

        function loadLogs() {
            const logs = JSON.parse(localStorage.getItem('rivalLogs')) || [];
            logHistory.innerHTML = logs.map(log => 
                `<div style="margin-bottom: 10px; border-bottom: 1px solid #222; padding-bottom: 5px;">
                    <span style="color: #666;">[${log.date}]</span> ${log.text}
                </div>`
            ).join('');
        }

        function saveLog() {
            const text = logInput.value;
            if (!text) return;

            const logs = JSON.parse(localStorage.getItem('rivalLogs')) || [];
            const newLog = {
                date: new Date().toLocaleTimeString(),
                text: text
            };
            
            logs.unshift(newLog); // Add to top
            localStorage.setItem('rivalLogs', JSON.stringify(logs));
            
            logInput.value = '';
            loadLogs();
        }
        loadLogs();

        // 4. FOCUS TIMER (30 MIN)
        let timerInterval;
        let timeLeft = 30 * 60; // 30 minutes in seconds

        function updateTimerDisplay() {
            const minutes = Math.floor(timeLeft / 60);
            const seconds = timeLeft % 60;
            document.getElementById('timer').innerText = 
                `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
        }

        function startTimer() {
            if (timerInterval) return; // Prevent multiple clicks
            timerInterval = setInterval(() => {
                if (timeLeft > 0) {
                    timeLeft--;
                    updateTimerDisplay();
                } else {
                    clearInterval(timerInterval);
                    alert("FOCUS SESSION COMPLETE. LOG YOUR PROGRESS.");
                }
            }, 1000);
        }

        function resetTimer() {
            clearInterval(timerInterval);
            timerInterval = null;
            timeLeft = 30 * 60;
            updateTimerDisplay();
        }

        // 5. BROWN NOISE GENERATOR (Simulated)
        let audioCtx;
        let noiseNode;
        let isPlaying = false;

        function toggleNoise() {
            if (isPlaying) {
                audioCtx.close();
                isPlaying = false;
                return;
            }

            // Simple Brown Noise via Web Audio API
            const AudioContext = window.AudioContext || window.webkitAudioContext;
            audioCtx = new AudioContext();
            const bufferSize = 4096;
            const brownNoise = (function() {
                var lastOut = 0;
                var node = audioCtx.createScriptProcessor(bufferSize, 1, 1);
                node.onaudioprocess = function(e) {
                    var output = e.outputBuffer.getChannelData(0);
                    for (var i = 0; i < bufferSize; i++) {
                        var white = Math.random() * 2 - 1;
                        output[i] = (lastOut + (0.02 * white)) / 1.02;
                        lastOut = output[i];
                        output[i] *= 3.5; // (roughly compensate for gain)
                    }
                }
                return node;
            })();

            brownNoise.connect(audioCtx.destination);
            isPlaying = true;
        }

    </script>
</body>
</html>
